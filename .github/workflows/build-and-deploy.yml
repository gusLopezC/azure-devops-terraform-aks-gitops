name: Build and Deploy to AKS

on:
  push:
    branches: [ main ]
    paths:
      - 'app/**'
      - '.github/workflows/build-and-deploy.yml'
  workflow_dispatch:

env:
  REGISTRY_NAME: ${{ secrets.REGISTRY_NAME }} # e.g. myacr
  IMAGE_NAME: flask-api
  RESOURCE_GROUP: ${{ secrets.RESOURCE_GROUP }}
  AKS_CLUSTER: ${{ secrets.AKS_CLUSTER }}
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }} # e.g. myacr.azurecr.io
  GITOPS_REPO: https://github.com/gusLopezC/azure-devops-terraform-aks-gitops.git
  GITOPS_BRANCH: main
  ARGOCD_APP_NAME: flask-api

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Log in to Azure Container Registry
        run: az acr login --name $REGISTRY_NAME

      - name: Build Docker image
        run: |
          docker build -t $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }} ./app

      - name: Push image to ACR
        run: |
          docker push $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }}

      - name: Get AKS credentials
        run: |
          az aks get-credentials --resource-group $RESOURCE_GROUP --name $AKS_CLUSTER --overwrite-existing

   #   - name: Deploy to AKS
   #     run: |
   #       kubectl set image deployment/flask-api \
   #         flask-api=$ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }} \
   #         -n default
   #       kubectl rollout status deployment/flask-api -n default

      - name: Update image tag in GitOps repo
        run: |
          git clone https://${{ secrets.GITOPS_REPO_TOKEN }}@github.com/gusLopezC/azure-devops-terraform-aks-gitops.git repo
          cd repo
          git remote set-url origin https://${{ secrets.GITOPS_REPO_TOKEN }}@github.com/gusLopezC/azure-devops-terraform-aks-gitops.git
          sed -i "s|image: .*flask-api.*|image: $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }}|" gitops-manifests/deployment.yaml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add gitops-manifests/deployment.yaml
          git commit -m "Update image tag to ${{ github.sha }} [skip ci]"
          git push origin $GITOPS_BRANCH

      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o argocd-linux-amd64 https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          sudo install -m 555 argocd-linux-amd64 /usr/local/bin/argocd
          rm argocd-linux-amd64
          argocd version --client

      - name: Get ArgoCD Password
        id: argocd-password
        run: |
          ARGOCD_PASSWORD=$(kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d)
          echo "password=$ARGOCD_PASSWORD" >> $GITHUB_OUTPUT

      - name: Setup ArgoCD Port Forward and Sync
        run: |
          # Esperar un momento para que ArgoCD detecte el cambio en el repo
          echo "Waiting for ArgoCD to detect changes in GitOps repo..."
          sleep 15
          
          # Verificar que la aplicación existe
          if ! kubectl get application $ARGOCD_APP_NAME -n argocd &>/dev/null; then
            echo "Warning: ArgoCD Application '$ARGOCD_APP_NAME' not found in namespace 'argocd'"
            echo "Please ensure the Application is created."
            exit 1
          fi
          
          # Iniciar port-forward en background
          kubectl port-forward svc/argocd-server -n argocd 8080:443 &
          PORT_FORWARD_PID=$!
          
          # Esperar a que el port-forward esté listo
          sleep 5
          
          # Login a ArgoCD usando localhost
          argocd login localhost:8080 --username admin --password "${{ steps.argocd-password.outputs.password }}" --insecure --grpc-web || {
            echo "Failed to login to ArgoCD"
            kill $PORT_FORWARD_PID 2>/dev/null || true
            exit 1
          }
          
          # Hacer refresh de la aplicación
          echo "Refreshing application..."
          argocd app get $ARGOCD_APP_NAME || echo "Could not get app details"
          
          # Hacer sync de la aplicación
          echo "Syncing application..."
          argocd app sync $ARGOCD_APP_NAME --prune --force || {
            echo "Sync failed, but continuing..."
          }
          
          # Esperar a que el sync se complete
          echo "Waiting for sync to complete..."
          argocd app wait $ARGOCD_APP_NAME --timeout 300 --health || {
            echo "Wait timeout or health check failed, but continuing..."
          }
          
          # Mostrar el estado de la aplicación
          echo ""
          echo "Application status:"
          argocd app get $ARGOCD_APP_NAME
          
          # Detener el port-forward
          kill $PORT_FORWARD_PID 2>/dev/null || true
